import { Text, View } from "react-native";

export default function Index() {
  return (
    <View className={"flex-1 flex-col items-center justify-center bg-gray-100"}>
      <Text className={"text-3xl color-amber-600 mb-3 font-bold"}>Start create app from scratch!</Text>
      <Text>with React native + Expo + NativeWind template</Text>
    </View>
  );
}
